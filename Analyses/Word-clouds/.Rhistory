7*20
3*25
7*15
6*15
6*15
140+74+105+19+19
140+74+105+90+90
9 + 31+ 43
library(tidyverse)
library(readxl)
#load data
longlist <- readxl_example("Word_Cloud_input.xls")
#load data
longlist <- readxl_example("Word_Cloud_input.xlsx")
# clear directory
rm(list=ls())
# set working directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
#load required packages
library(tidyverse)
library(readxl)
#load data
longlist <- readxl_example("Word_Cloud_input.xlsx")
#load data
longlist <- readxl_exel("Word_Cloud_input.xlsx")
xlsx_example <- readxl_example("Word_Cloud_input.xlsx")
read_excel(xlsx_example)
# set working directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
getwd()
#load data
D <- read.csv(file = "Word_Cloud_input.csv", header= TRUE, sep = ",", row.names = NULL)
View(D)
#load data
D <- read.csv(file = "Word_Cloud_input.csv", header= TRUE, sep = ";", row.names = NULL)
View(D)
D <- D[,1:2]
View(D)
#get table for synonyms:
syn <- table(D$Synonyms)
table(D$Synonyms)
#get table for synonyms:
syn <- as.data.frame(table(D$Synonyms))
View(syn)
names(syn) <- c("synonyms", "N")
View(syn)
syn <- syn %>% filter(synonyms != "")
#get table for related concepts:
rel <- as.data.frame(table(D$Related.concepts))
names(rel) <- c("related", "N")
rel <- rel %>% filter(related != "")
View(rel)
install.packages("wordcloud")
install.packages("wordcloud2")
library(wordcloud)
library(wordcloud2)
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=200, random.order=FALSE, rot.per=0.35,
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=25, random.order=TRUE, rot.per=0.35,
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0.35,
colors=brewer.pal(8, "Dark2"))
View(syn)
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0.35, scale=c(3.5,0.25)
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0, scale=c(1,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0, scale=c(6,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0, scale=c(5,0.25),
colors=brewer.pal(8, "Dark2"))
set.seed(1234) # for reproducibility
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26, random.order=TRUE, rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N, min.freq = 1,
max.words=26,
random.order=TRUE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
View(syn)
#seem like we have to tweek the N a little bit
syn$N2 <- ifelse(syn$N > 8, syn$N/2, syn$N)
#seem like we have to tweek the N a little bit
syn$N2 <- ifelse(syn$N >= 8, syn$N/2, syn$N)
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=TRUE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=TRUE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=TRUE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=TRUE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
set.seed(1234) # for reproducibility
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
set.seed(1234) # for reproducibility
#seem like we have to tweek the N a little bit
syn$N2 <- ifelse(syn$N >= 8, syn$N/2, syn$N)
cloud1 <- wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
cloud1
set.seed(1234) # for reproducibility
#seem like we have to tweek the N a little bit
syn$N2 <- ifelse(syn$N >= 8, syn$N/2, syn$N)
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
my_wordcloud<- wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud2(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud2(syn[, c(1,3)])
syn[, c(1,3)]
wordcloud2(syn[, c(1,3)])
wordcloud2(syn[, c(1,3)], size = 1, minSize = 0, gridSize =  0,)
wordcloud2(syn[, c(1,3)], size = 1, minSize = 0, gridSize =  0,
random.order = FALSE)
View(syn)
wordcloud2(syn[, c(1,3)], size = 2)
wordcloud2(syn[, c(1,3)], size = 0)
wordcloud2(syn[, c(1,3)], size = 1)
wordcloud2(syn[, c(1,3)], size = 0.5)
wordcloud2(syn[, c(1,3)], size = 0.2)
set.seed(1234) # for reproducibility
#seem like we have to tweek the N a little bit
syn$N2 <- ifelse(syn$N >= 8, syn$N/2, syn$N)
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
#related concepts
set.seed(1234) # for reproducibility
wordcloud2(rel, size = 01)
#related concepts
set.seed(1234) # for reproducibility
wordcloud2(rel, size = 01)
wordcloud2(rel, size = 01)
View(rel)
wordcloud2(rel, size = 0.5)
wordcloud2(rel, size = 0.5)
wordcloud2(rel, size = 0.6)
wordcloud2(rel, size = 0.7)
wordcloud2(rel, size = 0.6)
View(rel)
#seem like we have to tweek the N a little bit again
rel$N2 <- ifelse(rel$N >= 11, 8, rel$N)
rel$N2 <- ifelse(rel$N >= 11, 8, rel$N)
wordcloud2(rel[,c(1,3)], size = 0.6)
wordcloud2(rel[,c(1,3)], size = 0.7)
wordcloud2(rel[,c(1,3)], size = 0.7, widgetsize = 6)
wordcloud2(rel[,c(1,3)], size = 0.7, widgetsize =1)
wordcloud2(rel[,c(1,3)], size = 0.7, widgetsize = 0)
wordcloud2(rel[,c(1,3)], size = 0.7)
wordcloud2(rel[,c(1,3)], size = 0.7)
wordcloud2(rel[,c(1,3)], size = 0.7)
wordcloud2(rel[,c(1,3)], size = 0.5)
wordcloud2(rel[,c(1,3)], size = 0.6)
wordcloud2(rel[,c(1,3)], size = 0.65)
# set working directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
#load required packages
library(tidyverse)
library(readxl)
library(wordcloud)
library(wordcloud2)
#load data
D <- read.csv(file = "Word_Cloud_input.csv", header= TRUE, sep = ";", row.names = NULL)
D <- D[,1:2]
#--------------------------------------------------------------------------------------
# Data preparation
#get table for synonyms:
syn <- as.data.frame(table(D$Synonyms))
names(syn) <- c("synonyms", "N")
syn <- syn %>% filter(synonyms != "")
#get table for related concepts:
rel <- as.data.frame(table(D$Related.concepts))
names(rel) <- c("related", "N")
rel <- rel %>% filter(related != "")
#--------------------------------------------------------------------------------------
# creating the word clouds
#Synonyms
#seem like we have to tweek the N a little bit
syn$N2 <- ifelse(syn$N >= 8, syn$N/2, syn$N)
wordcloud(words = syn$synonyms, freq = syn$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=26,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
View(rel)
rel$N2 <- ifelse(rel$N >= 11, 8, rel$N)
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3.5,0.25),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3,0.25),
colors=brewer.pal(8, "Dark2"))
rel$N2 <- ifelse(rel$N >= 11, 8, rel$N)
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3,0.4),
colors=brewer.pal(8, "Dark2"))
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3,0.35),
colors=brewer.pal(8, "Dark2"))
set.seed(1234) # for reproducibility
#seem like we have to tweek the N a little bit again
rel$N2 <- ifelse(rel$N >= 11, 8, rel$N)
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
random.color = TRUE,
rot.per=0, scale=c(3,0.35),
colors=brewer.pal(8, "Dark2"))
rel$N2 <- ifelse(rel$N >= 11, 8, rel$N)
set.seed(1234) # for reproducibility
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(3,0.35),
colors=brewer.pal(8, "Dark2"))
set.seed(1254) # for reproducibility
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(3,0.35),
colors=brewer.pal(8, "Dark2"))
set.seed(1255) # for reproducibility
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(3,0.35),
colors=brewer.pal(8, "Dark2"))
set.seed(1255) # for reproducibility
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(3,0.3),
colors=brewer.pal(8, "Dark2"))
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(2.5,0.3),
colors=brewer.pal(8, "Dark2"))
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(2.5,0.4),
colors=brewer.pal(8, "Dark2"))
set.seed(1255) # for reproducibility
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(2.5,0.4),
colors=brewer.pal(8, "Dark2"))
set.seed(1265) # for reproducibility
#seem like we have to tweek the N a little bit again
wordcloud(words = rel$related, freq = rel$N2, min.freq = 1,
max.words=601,
random.order=FALSE,
#random.color = TRUE,
rot.per=0, scale=c(2.5,0.4),
colors=brewer.pal(8, "Dark2"))
